{
  "name": "claimward-app-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Svelte UI for the Claimward macOS app, served inside the tray's webview",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.0.0",
    "vite": "^7.0.0"
  }
}
